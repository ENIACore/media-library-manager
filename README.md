# media_library_manager

# TODO
- Add filter to parser causing non-recognized files to be skipped
- Add exclusitivity options, i.e season dir can have subtitle file OR subtitle dir but not both (classifier)
- Add subtitle patterns (classifier)
- Add validation subtitle dir has children (classifier)
